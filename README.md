# Baking Recipes App

Simple static web app with `index.html`, `script.js` and `style.css`.

## Run

Open `index.html` in your browser.

## Notes

- This repository was initialized locally and committed with a basic .gitignore.
- If you'd like this pushed to a remote (GitHub or similar), tell me and I can help create the remote and set it up.